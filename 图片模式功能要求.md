图片模式的功能要求

0、加载图片时（包括拖拽图片进入窗口、通过菜单打开图片、上一张与下一张切换等所有的方式），此时要保持窗口原本尺寸不变（注意，不是锁定窗口，只是此时保持窗口大小不变），以便图片计算缩放比例，适应窗口，完全显示在窗口中央；


1、滚轮缩放功能
##用鼠标滚轮可以缩放图片：当使用鼠标滚轮缩放时，窗口自动适应图片尺寸，跟随图片同步缩放（即用户只见图片不见窗口的效果）；

##缩放极限控制规则：
- 图片极限：最大放大至原图10倍，最小缩小至原图5%；
- 窗口极限：缩小时，长边缩小至200像素时停止（短边可以小于200像素）；放大时，"后一条边"放大至屏幕尺寸后停止（前一条边可以超过屏幕尺寸）；

//*
关于窗口放大时“后一条边”的计算逻辑如下：
-设图片的宽边为w，高边为h，设屏幕的宽为m，高为n；
-计算宽度系数：a=w/m；计算高度系数：b=h/n；
-比较a与b：当a<b时，此图片的宽边w是后一条边；当a>b时，此图片的高边h是后一条边。
*//

- 如果图片先达到极限值不影响窗口缩放，而如果窗口先达到极限值，也不影响图片缩放；如果回到极限范围内，继续遵循前面的规则（即窗口适应图片）。


2、同目录图片切换功能
## 点击窗口左右边缘10%区域，可以切换上一张和下一张（快捷键为左右方向键）；
## 要求无论通过任何打开方式（包括菜单打开、拖入窗口打开、文件右键菜单打开等等），都能读取到同目录的图片供切换。
## 如果目录内到达最后一张边界或最前一张边界，则循环即可。


3、窗口尺寸锁定功能
## 双击可锁定窗口尺寸，再双击可解锁；
## 当窗口尺寸被锁定状态下，鼠标滚轮单独缩放图片；

## 在窗口尺寸锁定状态下，并按住空格键时，鼠标变成手型，可单独拖拽图片在窗口内任意移动（窗口即遮罩，显示在窗口内的部分），停止拖拽时图片位置保持；
- 加载新图片后（包括拖入、菜单打开、切换上一张下一张等各种方式加载），图片位置重置，尺寸复位，显示在窗口中央；
- 窗口尺寸解锁后，图片尺寸复位，显示在窗口中央。



###注意###：
- 图片模式下，不需要显示控制栏（控制栏仅在音频和视频模式下显示和使用）；
- 菜单栏的显示隐藏逻辑与视频模式完全相同；
- 程序整体的拖拽逻辑与视频模式完全相同（在窗口内任意位置都要可以拖拽移动）；
- 注意不要破坏窗口控制按钮（最小化、最大化、关闭）的功能；
- 注意不要破坏原本鼠标调节窗口大小的功能。




