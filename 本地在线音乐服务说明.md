# 本地在线音乐服务说明

## 目的

为桌面端在线音乐功能提供一个本地服务层，统一封装搜索与播放地址获取逻辑，避免前端直接请求第三方音乐接口导致的不稳定问题。

## 当前本地服务文件

- `scripts/local-music-server.mjs`
- `scripts/dev-with-music-server.mjs`

## 当前端口

- `127.0.0.1:31999`

## 当前接口

### 健康检查

- `GET /api/music/health`

### 搜索歌曲

- `GET /api/music/search?keyword=关键词&source=netease`

返回结构示例：

```json
{
  "results": [
    {
      "id": "123456",
      "title": "歌曲名",
      "artist": "歌手",
      "source": "netease",
      "streamUrl": "/api/music/stream?id=123456&source=netease"
    }
  ]
}
```

### 播放流代理

- `GET /api/music/stream?id=歌曲ID&source=netease`

该接口会向上游获取真实音频地址，并以本地服务形式转发给前端播放器。

## 开发方式

### 单独启动本地音乐服务

```bash
npm run dev:music
```

### Tauri 开发模式

Tauri 开发前会自动执行：

```bash
node scripts/dev-with-music-server.mjs
```

该脚本会同时启动：

- 本地音乐服务
- Vite 前端开发服务

## 后续建议

- 后面如果要接 Listen1 完整 SDK，可以直接替换 `local-music-server.mjs` 内部实现
- 前端尽量继续只调用本地服务，不直接依赖第三方平台接口
- 如果后续需要远程部署，只需要把本地服务迁移到服务器并把前端服务地址配置化
